# 快速开始

## 最小流程

1. 在工作区选择输入类型，例如 `Mod JAR`。
2. 先选择源语言和目标语言。
3. 选择一个翻译器。
4. 点击 `运行预检`，先确认输入可处理。
5. 再点击 `开始生成`。

## 建议

- 第一次使用可以先用 `glossary` 或 `deep-free` 跑通流程。
- 如果预检出现阻断，优先先修正阻断项，不要直接忽略。
- 处理完成后，可在结果页、翻译报告、任务历史里继续排查。
